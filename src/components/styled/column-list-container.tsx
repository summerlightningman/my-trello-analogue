import styled from "styled-components";

const ColumnListContainer = styled.ul`
  display: flex;
  flex-flow: row;
  align-items: flex-start;
  overflow-x: auto;
  height: 80vh;
`;

export default ColumnListContainer